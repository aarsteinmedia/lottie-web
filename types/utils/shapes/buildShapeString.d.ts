import type { Matrix } from '../../utils/Matrix';
import type { ShapePath } from '../../utils/shapes/ShapePath';
export declare function buildShapeString(pathNodes: ShapePath, length: number, closed: boolean, mat: Matrix): string;
